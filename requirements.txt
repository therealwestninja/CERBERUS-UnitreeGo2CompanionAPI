# CERBERUS — requirements.txt
# ══════════════════════════════════════════════════════════════════════════════
# Pin minor versions for reproducibility; patch versions float for security.
# Install: pip install -r requirements.txt
# ══════════════════════════════════════════════════════════════════════════════

# ── Web / API server ──────────────────────────────────────────────────────────
fastapi>=0.111.0,<0.200.0
uvicorn[standard]>=0.29.0,<0.35.0
pydantic>=2.7.0,<3.0.0
python-multipart>=0.0.9          # file upload support
websockets>=12.0,<14.0
aiohttp>=3.9.0,<4.0.0            # WebRTC signaling HTTP POST

# ── WebRTC transport (Go2 PRO/AIR) ───────────────────────────────────────────
aiortc>=1.9.0,<2.0.0             # RTCPeerConnection + data channel

# ── BLE / peripheral devices (Hismith, Galaxy Fit 2) ─────────────────────────
bleak>=0.21.0,<0.23.0            # Cross-platform BLE (Windows/macOS/Linux)

# ── Buttplug.io ───────────────────────────────────────────────────────────────
buttplug>=1.0.0                  # Official PyPI package, protocol v4

# ── UI (Dear PyGui) ───────────────────────────────────────────────────────────
dearpygui>=1.11.0,<2.0.0         # Native GPU-accelerated UI

# ── Config / serialisation ────────────────────────────────────────────────────
pyyaml>=6.0.1,<7.0.0
python-dotenv>=1.0.0

# ── Utilities ─────────────────────────────────────────────────────────────────
# (no heavy deps — keep it lean)

# ── Optional: Vision / Perception ────────────────────────────────────────────
# ultralytics>=8.0.0            # YOLOv8 object detection
# mediapipe>=0.10.0             # Pose / landmark detection
# opencv-python>=4.9.0          # Camera capture

# ── Optional: ROS2 ───────────────────────────────────────────────────────────
# rclpy                         # Installed via apt / ROS2 overlay, not pip

# ── Dev / Testing ─────────────────────────────────────────────────────────────
pytest>=8.1.0
pytest-asyncio>=0.23.0
httpx>=0.27.0                    # FastAPI async test client
ruff>=0.4.0                      # Linter / formatter
