# go2_platform/requirements.txt
# ══════════════════════════════════════════════════════════════════════════
# Core dependencies — pinned for reproducibility
# ══════════════════════════════════════════════════════════════════════════

# Web framework + server
fastapi>=0.111.0,<0.200.0
uvicorn[standard]>=0.29.0,<0.35.0
pydantic>=2.7.0,<3.0.0
python-multipart>=0.0.9          # file uploads

# Real-time communication
websockets>=12.0,<14.0

# HTTP client (fleet manager, OTA)
aiohttp>=3.9.0,<4.0.0

# Configuration + serialization
pyyaml>=6.0.1,<7.0.0

# Utilities
python-dotenv>=1.0.0             # .env file loading

# ── Optional: Vision ─────────────────────────────────────────────────────
# ultralytics>=8.0.0             # YOLOv8 object detection
# mediapipe>=0.10.0              # Pose/landmark detection
# opencv-python>=4.9.0           # Camera capture

# ── Optional: BLE / Connectivity ─────────────────────────────────────────
# bleak>=0.21.0                  # Cross-platform BLE

# ── Optional: ROS2 ───────────────────────────────────────────────────────
# rclpy                          # Installed via ROS2 apt packages

# ── Dev / Testing ─────────────────────────────────────────────────────────
pytest>=8.1.0
pytest-asyncio>=0.23.0
httpx>=0.27.0                    # FastAPI test client
