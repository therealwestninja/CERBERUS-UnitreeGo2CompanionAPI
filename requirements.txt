# CERBERUS v3.2.0 — Core requirements
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
pydantic>=2.7.0
websockets>=12.0
aiofiles>=23.2.0
pyyaml>=6.0.1
python-multipart>=0.0.9
python-dotenv>=1.0.0
numpy>=1.26.0

# Testing
pytest>=8.1.0
pytest-asyncio>=0.23.0
httpx>=0.27.0

# ── Optional — install the extras you need ─────────────────────────────────
# Go2 EDU wired (CycloneDDS):
#   pip install unitree_sdk2py
#
# Go2 Wi-Fi WebRTC (all models, no jailbreak):
#   pip install go2_webrtc_connect
#
# BLE peripherals (Hismith, Galaxy Fit 2):
#   pip install bleak
#
# Buttplug / Intiface Central:
#   pip install buttplug
