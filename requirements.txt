# CERBERUS — Unitree Go2 Companion API
# requirements.txt  (v3.1.0)
# ════════════════════════════════════════════════════════════════════════

# ── Core web stack ───────────────────────────────────────────────────────
fastapi>=0.111.0,<0.200.0
uvicorn[standard]>=0.29.0,<0.35.0
pydantic>=2.7.0,<3.0.0
python-multipart>=0.0.9        # file uploads
websockets>=12.0,<14.0
aiohttp>=3.9.0,<4.0.0          # HTTP client + LLM fallback
pyyaml>=6.0.1,<7.0.0
python-dotenv>=1.0.0
numpy>=1.26.0                  # sensor math

# ── Dev / testing ─────────────────────────────────────────────────────────
pytest>=8.1.0
pytest-asyncio>=0.23.0
httpx>=0.27.0
ruff>=0.4.0

# ════════════════════════════════════════════════════════════════════════
# OPTIONAL — install the group(s) you need:
#
#   Go2 EDU (wired CycloneDDS):
#     pip install unitree_sdk2py
#     NOTE: also requires cyclonedds==0.10.2 built from source
#     See: https://github.com/unitreerobotics/unitree_sdk2_python
#
#   Go2 AIR/PRO/EDU Wi-Fi (WebRTC, no jailbreak):
#     pip install go2_webrtc_connect
#     OR:  pip install unitree_webrtc_connect
#
#   Vision / Perception:
#     pip install ultralytics mediapipe opencv-python
#
#   Audio (Go2 Pro/EDU built-in speaker/mic):
#     sudo apt install portaudio19-dev
#     pip install PyAudio SpeechRecognition
#
#   LLM NLU fallback (OpenAI-compatible):
#     pip install openai          # or set CERBERUS_OPENAI_API_KEY
#     (aiohttp already included above)
#
#   ROS2 (optional — install via apt, not pip):
#     sudo apt install ros-humble-rclpy ros-humble-std-msgs
# ════════════════════════════════════════════════════════════════════════

# ── Server static file support ─────────────────────────────────────────────
aiofiles>=23.2.0               # required by FastAPI StaticFiles
